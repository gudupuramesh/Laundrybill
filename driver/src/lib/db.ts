/**
 * Firestore handle — JS Firebase SDK via the compatibility facade in `./firebase`.
 * `firestore()` returns the chained-API facade (`.collection().doc().get()`, `.batch()`).
 */
export { firestore } from './firebase';
